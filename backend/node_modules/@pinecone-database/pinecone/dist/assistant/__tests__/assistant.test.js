"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const index_1 = require("../index");
const errors_1 = require("../../errors");
describe('Assistant constructor validation', () => {
    const mockConfig = {
        apiKey: 'test-key',
    };
    test('throws error when assistant name is empty string', () => {
        expect(() => {
            new index_1.Assistant({ name: '' }, mockConfig);
        }).toThrow(errors_1.PineconeArgumentError);
        expect(() => {
            new index_1.Assistant({ name: '' }, mockConfig);
        }).toThrow('Assistant name is required and cannot be empty.');
    });
    test('throws error when assistant name is whitespace only', () => {
        expect(() => {
            new index_1.Assistant({ name: '   ' }, mockConfig);
        }).toThrow(errors_1.PineconeArgumentError);
        expect(() => {
            new index_1.Assistant({ name: '   ' }, mockConfig);
        }).toThrow('Assistant name is required and cannot be empty.');
    });
    test('creates assistant successfully with valid name', () => {
        expect(() => {
            new index_1.Assistant({ name: 'valid-assistant-name' }, mockConfig);
        }).not.toThrow();
    });
    test('creates assistant successfully with valid name and host', () => {
        expect(() => {
            new index_1.Assistant({ name: 'valid-assistant-name', host: 'https://test-host.com' }, mockConfig);
        }).not.toThrow();
    });
});
//# sourceMappingURL=assistant.test.js.map